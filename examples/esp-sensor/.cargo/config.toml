[target.riscv32imac-unknown-none-elf]
runner    = "espflash flash --monitor --chip esp32c6"

[build]
rustflags = [
  "-C", "link-arg=-Tlinkall.x",
  "-C", "force-frame-pointers",
]

target = "riscv32imac-unknown-none-elf"

[env]
ESP_LOG = "debug,esp_rtos=off,esp_radio=off,esp_hal=off"

[unstable]
build-std = ["core", "alloc"]
