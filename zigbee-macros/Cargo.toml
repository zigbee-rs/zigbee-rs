[package]
name = "zigbee-macros"
version.workspace = true

description = "Declarative macros backing the `zigbee` protocol stack: frame (de)serialization and information bases"
documentation = "https://docs.rs/zigbee-macros"
readme = "README.md"
keywords = ["zigbee", "macros", "no-std", "embedded"]
categories = ["no-std", "embedded", "development-tools"]
license = "MIT OR Apache-2.0"

authors.workspace = true
repository.workspace = true
edition.workspace = true

[dependencies]

[dev-dependencies]
byte.workspace = true

[package.metadata.docs.rs]
all-features = true
